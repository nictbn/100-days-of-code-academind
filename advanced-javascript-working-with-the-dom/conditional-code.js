const myName = 'Max';

if (myName === 'Max') {
    console.log('Hello');
}

const isLoggedIn = false;

if (!isLoggedIn) {
    console.log('User is not logged in!');
}

const enteredUserName = 'Maxumilian';
// if (enteredUserName.length > 0) {
//     console.log('Input is valid!')
// }

if (enteredUserName) {
    console.log('Input is valid!')
}